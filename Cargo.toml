[package]
name = "remun"
version = "0.1.0"
edition = "2021"

[dependencies]
asmnes = { workspace = true }
shared = { workspace = true }

[workspace]
members = [ "asmnes", "shared" ]

[workspace.dependencies]
strum = "0.26"
strum_macros = "0.26"

shared = { path = "shared" }
asmnes = { path = "asmnes" }

