[package]
name = "remun"
version = "0.1.0"
edition = "2021"

[dependencies]
asmnes = { workspace = true }
shared = { workspace = true }

[workspace]
members = [ "asmnes", "shared" ]

[workspace.dependencies]
shared = { path = "shared" }
asmnes = { path = "asmnes" }

